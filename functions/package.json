{
  "name": "1peace-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@genkit-ai/googleai": "^1.16.0",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.6.0",
    "genkit": "^1.16.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
